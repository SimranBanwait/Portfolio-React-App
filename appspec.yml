version: 0.0
os: linux
files:
   - source: /
     destination: /Workspace/Pipeline

hooks:
   BeforeInstall:
    - location: scriptss/beforeinstall.sh
      runas: root
      timeout: 300
   ApplicationStart:
    - location: scriptss/afterinstall.sh
      runas: root
      timeout: 300